# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
next-env.d.ts
out/
.turbo/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
.claude/audit.log

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Test / coverage
coverage/

# NOTE: data/synthetic/ is intentionally NOT ignored — generated datasets are
# committed as versioned fixtures (clearly labelled synthetic). See BUILD_PLAN.md.
# EXCEPT the intake queue, which is mutable runtime state written by the web app:
data/synthetic/intake/
# and operator approvals (runtime state written by the console):
data/synthetic/approvals/
