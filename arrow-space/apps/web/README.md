# @arrow-space/web

**Phase 1 — the front door.** Institutional site (Next.js App Router + Tailwind, deploy to Vercel):
authorization-led hero, Authorizations & Quality trust spine, capabilities, markets (defense-first),
AOG desk page. Retail signals stripped. Structured RFQ + AOG intake forms validate against the
`RFQ` schema in `@arrow-space/schema` and write objects to the intake queue.

This is a placeholder until Phase 1 (BUILD_PLAN.md §6, tasks 6–10). It deliberately has no scripts
yet, so Turborepo skips it.
