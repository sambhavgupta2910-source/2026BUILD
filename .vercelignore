# Deploy only what the preview needs. The deployed server fetches the real
# DLD dataset from its Drive default URL at boot; the bundled demo CSV ships
# only as a clearly-ribboned fallback for when that fetch fails.
.git
node_modules
_codex
presentation
*.pptx
*.pdf
# real DLD pulls and captured leads never ship in a preview bundle
transactions-dld.csv
projects-dld.json
.data
.cache
.env
